language: java
sudo: false
dist: trusty
install: true

addons:
    firefox: latest
    apt:
        packages:
            - google-chrome-stable

before_script:
    - export DISPLAY=:99.0
    - sh -e /etc/init.d/xvfb start &
    - sleep 3

script:
    - mvn test
